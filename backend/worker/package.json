{
  "name": "tracky-worker",
  "version": "2.0.0",
  "private": true,
  "description": "Cloudflare Worker backend for Tracky - Gemini + USDA proxy",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.28.0"
  },
  "dependencies": {
    "zod": "^3.22.4"
  }
}
